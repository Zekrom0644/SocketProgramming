# ☁️ 클라우드 계산기 (Cloud Calculator)

## 📖 프로젝트 설명

자바 소켓 프로그래밍과 스레드 풀(Thread Pool)을 이용하여 여러 클라이언트의 요청을 동시에 처리할 수 있는 **멀티 스레딩 계산기 서버**입니다.

---

## ⚙️ 주요 기능

* **TCP/IP 소켓 통신:** 자바 `ServerSocket`과 `Socket`을 이용한 클라이언트-서버 구현
* **멀티 스레딩:** `ExecutorService` (CachedThreadPool)를 사용한 효율적인 클라이언트 연결 처리
* **프로토콜 기반 연산:** 사칙연산(ADD, SUB, MUL, DIV) 및 예외 처리 (0으로 나누기, 잘못된 인자 등)

---

## ▶️ 실행 방법

1.  `CalculatorServer.java`를 컴파일하고 실행하여 서버를 시작합니다.
2.  `CalculatorClient.java`를 컴파일하고 실행하여 클라이언트를 연결합니다.

---

## ⌨️ 사용법 (프로토콜 예시)

클라이언트 화면에서 정의된 프로토콜에 맞춰 명령어를 입력합니다.

**형식:** `[명령어] [숫자1] [숫자2]`

### 성공 예시 (덧셈)
